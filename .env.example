# ──────────────────────────────────────────────────────────────────────────────
# .env.example  —  TEMPLATE ONLY — DO NOT put real values here.
#
# Copy to .env for local dev:   cp .env.example .env
# Then fill in real values.
#
# In production, ALL of these variables must be set in Vercel
# (Project Settings → Environment Variables).
# ──────────────────────────────────────────────────────────────────────────────

# ── Database ────────────────────────────────────────────────────────────────
# Neon / PostgreSQL POOLED connection string (used at runtime by Prisma Client).
# Format: postgresql://USER:PASSWORD@HOST-pooler/DBNAME?sslmode=require&channel_binding=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.region.aws.neon.tech/DBNAME?sslmode=require&channel_binding=require"

# Neon / PostgreSQL DIRECT connection string (used by Prisma schema engine for migrations).
# Same credentials — only the hostname differs (no -pooler suffix).
DIRECT_URL="postgresql://USER:PASSWORD@HOST.region.aws.neon.tech/DBNAME?sslmode=require&channel_binding=require"

# ── NextAuth ────────────────────────────────────────────────────────────────
# Generate with:  openssl rand -hex 32
NEXTAUTH_SECRET="REPLACE_WITH_64_CHAR_HEX_STRING"

# In production this MUST match your Vercel deployment domain exactly.
# Example: https://your-school-portal.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# ── CSRF Protection ──────────────────────────────────────────────────────────
# Generate with:  openssl rand -hex 32
CSRF_SECRET="REPLACE_WITH_64_CHAR_HEX_STRING"

# ── SMTP / Email ─────────────────────────────────────────────────────────────
# Used by Nodemailer for sending password-reset emails.
SMTP_HOST="smtp.youremail.com"
SMTP_PORT="587"
SMTP_USER="your@email.com"
# ── Upstash / Redis ──────────────────────────────────────────────────────────
# Used for distributed rate limiting.
UPSTASH_REDIS_REST_URL="https://your-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-token"

